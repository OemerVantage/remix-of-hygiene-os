

## Plan: Shopify Store erneut verbinden

Die Store-Owner E-Mail wurde aktualisiert. Ich werde jetzt erneut die Shopify-Integration mit `store_type: "existing"` aufrufen, um den Store `mp0y11-0r.myshopify.com` zu verbinden.

### Schritt
- `enable_shopify` mit `store_type: "existing"` aufrufen, um den OAuth-Flow zu starten.

